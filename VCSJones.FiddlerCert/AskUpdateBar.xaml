<UserControl x:Class="VCSJones.FiddlerCert.AskUpdateBar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:VCSJones.FiddlerCert"
             mc:Ignorable="d" 
             d:DesignHeight="30" d:DesignWidth="400" Background="Beige">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <TextBlock TextWrapping="Wrap" Margin="5,0" VerticalAlignment="Center">Would you like to enable automatic update checks for Fiddler Cert Inspector?</TextBlock>
        <Button Grid.Column="1" Command="{Binding YesCommand}" MinWidth="75" Margin="3" Padding="3">Yes</Button>
        <Button Grid.Column="2" Command="{Binding NoCommand}" MinWidth="75" Margin="3" Padding="3">No</Button>
    </Grid>
</UserControl>
